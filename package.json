{
  "name": "atishay-kasliwal.github.io",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "@mlc-ai/web-llm": "^0.2.80",
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-helmet": "^6.1.0",
    "react-router-dom": "^6.30.1",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev:dashboard": "cd \"UI interface/apps/web\" && npm run dev",
    "build:dashboard": "node scripts/build-and-copy-dashboard.cjs",
    "build": "npm run build:dashboard && node scripts/copy-dashboard-routes.cjs && react-scripts build && cp build/index.html build/404.html && (cp public/_redirects build/_redirects 2>/dev/null || cp _redirects build/_redirects 2>/dev/null || true) && cp public/_headers build/_headers 2>/dev/null || true",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": "https://atishaykasliwal.com",
  "devDependencies": {
    "wrangler": "^4.45.3"
  }
}
